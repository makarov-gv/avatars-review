# Paper manuscript sources (kept local; used only by scripts/_bootstrap_methods.py
# to regenerate data/methods.json and the figures, which are committed).
.paper/

# Build/maintenance tooling and source data (kept local; the generated README and
# figures/ are what ships). Regenerate locally with scripts/generate_readme.py.
scripts/
data/

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS / editor
.DS_Store
.idea/
.vscode/
